name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  Linter-run:
    name: Run Linter
    runs-on: ubuntu-latest

    steps:
    - name: Super-Linter
      uses: super-linter/super-linter@v5.7.2
    
  Docker:
    name: Run Node
    runs-on: ubuntu-latest

    steps:
     - name: Checkout
       uses: actions/checkout@v4.1.1

     - name: Build and Push Docker Image
       id: docker_build
       uses: mr-smithers-excellent/docker-build-push@v4
       with:
        image: hussamgamal/node-img
        tag: v1
        registry: docker.io
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

  Terraform:
    name: Terraform Deployment
    runs-on: ubuntu-latest
    needs: Docker

    steps:
    - name: Checkout Terraform
      uses: actions/checkout@v4.1.1

    - name: Verify Kubeconfig Content
      run: echo "${{ secrets.KUBECONFIG_CONTENT }}"

    - name: Setup Kubeconfig
      run: |
        mkdir -p $HOME/.kube
        touch $HOME/.kube/config
        echo "${{ secrets.KUBECONFIG_CONTENT }}" | base64 -d > $HOME/.kube/config

    
    - name: Debugging Info
      run: |
        pwd
        ls -R
        tree -L 3
        kubectl config viewthis is the result
        
    - name: Initialize Terraform
      run: |
        cd TerraformDocker
        terraform init

    - name: Terraform Plan
      run: |
        cd TerraformDocker
        terraform plan

    - name: Terraform Validate
      run: |
        cd TerraformDocker
        terraform validate

    - name: Terraform Apply
      run: |
        cd TerraformDocker
        terraform apply --auto-approve
      env:
          KUBECONFIG: $HOME/.kube/config
